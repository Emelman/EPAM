@model IEnumerable<UsersAndRewards.Shared.RewardViewModel>

@{
    ViewBag.Title = "Rewards";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Reward ID
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RewardTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RewardDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RewardId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RewardTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RewardDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
